# Initial Setup

1. Ask `xela6134` for .env file
2. Create a `.env` file within `/databases` and `/backend`
3. Copy and paste the contents copied from `xela6134`
4. `pip install -r requirements.txt` (or `pip3 install -r requirements.txt`)
    - `requirements.txt` are modules for Python
